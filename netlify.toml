# Enable remote images for Netlify image CDN and local dev
[images]
remote_images = ["i.pravatar.cc"]
[build]
  command = "pnpm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
